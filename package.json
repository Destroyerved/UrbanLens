{
  "name": "urbanlens-root",
  "version": "0.1.0",
  "private": true,
  "description": "UrbanLens — AI-Powered Urban Planning & Land Intelligence Platform",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "setup": "npm install --prefix web && (python3 -m pip install -r backend/requirements.txt || python -m pip install -r backend/requirements.txt)",
    "engine": "python3 -m uvicorn app.main:app --port 8000 --app-dir backend || python -m uvicorn app.main:app --port 8000 --app-dir backend",
    "web": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "start": "npm run start --prefix web",
    "lint": "npm run lint --prefix web",
    "demo": "node web/scripts/demo-run.mjs",
    "demo:host": "node scripts/demo-host.mjs",
    "verify:ui": "node web/scripts/verify-ui.mjs"
  }
}
