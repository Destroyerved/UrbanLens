# UrbanLens spatial backend — PRD §41–44
fastapi>=0.115
uvicorn[standard]>=0.27
pydantic>=2.7

# Spatial. GeoPandas is deliberately not required: layers arrive as GeoJSON and
# shapely + pyproj cover every operation used here, which keeps the install
# working on machines without GDAL.
shapely>=2.0
pyproj>=3.6

# Analysis + ML
numpy>=1.26
pandas>=2.2
scikit-learn>=1.4
xgboost>=2.0

# Raster IO for the DEM (flood risk) and Sentinel-2 (thermal, NDVI).
# Was imported by app/gis/flood.py and app/thermal.py without being declared,
# so a fresh `npm run setup` produced an engine where /api/health, /api/wards,
# /api/overview, /api/growth and /api/zoning/conflicts all returned 500.
rasterio>=1.3

# Report generation (POST /api/report -> PDF)
reportlab>=4.0
